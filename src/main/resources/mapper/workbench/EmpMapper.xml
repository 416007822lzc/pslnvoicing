<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.pslnvoicing.mapper.workbench.EmpMapper">

    <select id="queryEmpList" resultType="com.example.pslnvoicing.vo.workbench.EmpVo">
        select a.emp_id,a.emp_name,a.emp_gender,a.emp_phone,a.emp_password,b.dept_name from personnel_emp a left join personnel_dept b on a.dept_id = b.dept_id;
    </select>

    <select id="login" resultType="integer">
        select count(*) from personnel_emp where emp_phone = #{param1} and emp_password = #{param2}
    </select>
</mapper>